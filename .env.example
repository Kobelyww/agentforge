# ===== AgentForge 环境变量示例 =====
# 复制为 .env 并按需填写。所有 LLM Key 均可选 —— 不配置任何 Key 时，
# 平台自动使用内置 Mock Provider，可完整体验 Agent / RAG / 工具调用全流程。

# ---- LLM Providers（OpenAI 兼容协议，适用于 OpenAI / GLM / Kimi / DeepSeek / Qwen 等）----
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 智谱 GLM
GLM_API_KEY=
GLM_MODEL=glm-4-plus

# 华为云 ModelArts MaaS（OpenAI 兼容端点）
MODELARTS_API_KEY=
MODELARTS_BASE_URL=https://api.modelarts-maas.com/v1
MODELARTS_MODEL=

# Anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# ---- Server ----
AGENTFORGE_HOST=0.0.0.0
AGENTFORGE_PORT=8000
AGENTFORGE_LOG_LEVEL=INFO
# 配置后所有 /api/* 接口要求请求头 X-API-Key
AGENTFORGE_API_KEY=
# 每客户端每分钟最大请求数
AGENTFORGE_RATE_LIMIT_RPM=60

# ---- 认证（二选一或并用）----
# JWT 登录：设置后启用 POST /api/auth/login（PBKDF2 存储，Bearer Token 2h）
AGENTFORGE_ADMIN_USERNAME=admin
AGENTFORGE_ADMIN_PASSWORD=
# 跨重启保持 JWT 有效（默认从密码派生）
AGENTFORGE_AUTH_SECRET=

# ---- 运维 ----
# 工单创建事件的 Webhook 地址（POST {event, work_order}）
AGENTFORGE_WEBHOOK_URL=
